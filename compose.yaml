name: spark-image-lab
services:
  lab:
    build: .
    image: spark-image-lab:local
    platform: linux/arm64
    gpus: all
    ipc: host
    init: true
    user: "${LOCAL_UID:-1000}:${LOCAL_GID:-1000}"
    ports:
      - "127.0.0.1:${SPARK_HTTP_PORT:-7862}:7860"
    volumes:
      - .:/workspace
    environment:
      SPARK_HOST: 0.0.0.0
      SPARK_PORT: "7860"
      GRADIO_ANALYTICS_ENABLED: "False"
      HF_HUB_DISABLE_TELEMETRY: "1"
    stop_grace_period: 90s
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:7860/', timeout=5)"]
      interval: 30s
      timeout: 10s
      start_period: 6m
      retries: 3
