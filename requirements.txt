transformers==5.17.0
accelerate==1.15.0
gradio==6.28.0
pillow==12.3.0
# Optional rewriter decode kernels. Harmless if absent (transformers falls back
# to reference PyTorch); the causal_conv1d wheel is ABI-incompatible with the
# NGC torch 2.14.0a0 build (undefined symbol) until it builds against it.
flash-linear-attention>=0.5.2
causal_conv1d
diffusers @ git+https://github.com/huggingface/diffusers@80c7ed262aeffbeb43ef13ae04baeb9b84515a69
